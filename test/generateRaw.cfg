process TEST = {
       	untracked PSet maxEvents = {untracked int32 input = 1}

	source = DaqSource{
	 untracked string readerPluginName = "DaqFakeReader"
	 untracked PSet readerPset = {}
	}

	module dummyunpacker = DummyUnpackingModule{ }

	module out = PoolOutputModule {
        	untracked string fileName =".rawdata.root"
	}

#	module out = AsciiOutputModule {
#        	untracked string fileName =".rawdata.root"
#	}
	 
	endpath ep = {out}
	path p = {dummyunpacker}
}
